$.ajax() 메서드
: 서버에서 지정한 형식의 자료를 요청하는 제이쿼리 범용 메서드입니다.
XML, JSON, TEXT등 다양한 타입의 자료를 요청할 수 있으며 형식은
다음과 같습니다.

$.ajax({
  url : '가져올 파일명',
  dataType : '데이터 형식',
  succecc : function() {
    // 콜백 함수(가져온 파일에 대한 처리)
  }
});